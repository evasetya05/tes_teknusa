# This will make the post_media package a proper Python package
# and ensure the default app config is used when the app is included in INSTALLED_APPS

default_app_config = 'post_media.apps.PostMediaConfig'